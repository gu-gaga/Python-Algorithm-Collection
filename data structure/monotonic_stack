# 单调栈
n=int(input())
stack=[]
for a in map(int,input().split()):
    while stack and stack[-1]>=a:
        stack.pop()
    if stack:
        print(stack[-1],end=' ')
    else:
        print(-1,end=' ')
    stack.append(a)
